const a = 5;
const b = 7;
const c = 2;
const d = 3;
if ( a + b > c + d) {
 document.write(`a + b (${a + b}) es mayor que c + d (${c + d})`); 
  
} else{ 
 document.write(`a + b (${a + b}) es menor que c + d (${c + d})`);  
}
